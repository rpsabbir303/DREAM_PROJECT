This folder is reserved for future Python AI runtime and orchestration adapters.

Planned modules:
- `python-gateway/` for local model bridge
- `workers/` for long-running automation jobs
- `connectors/` for external tool adapters
