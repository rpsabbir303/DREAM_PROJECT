/** Compact JARVIS mark for assistant message headers (message UI only). */
export function ChatJarvisMark() {
  return (
    <span className="jarvis-msg-mark" aria-hidden>
      <span className="jarvis-msg-mark-core" />
    </span>
  )
}
